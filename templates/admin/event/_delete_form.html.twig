<form method="post" action={{ path('admin_event_delete', {id: event.id} ) }} onsubmit="return confirm('Are you sure you want to delete this item?');" class="flex justify-center">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
	<button class="btn-delete">
		Supprimé l'event
	</button>
</form>
