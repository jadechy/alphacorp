{% extends 'admin/base_admin.html.twig' %}

 {% block admin_title %}Langue {{language.name}}{% endblock %}
  {% set return_path = 'admin_language_homepage' %} 

{% block admin_content %}
         <section>
 

                 <table class="mt-8">
                    <thead>
                        <tr >
                            <th scope="col" > Key</th>
                            <th scope="col" > Value</th> 
                        </tr>
                    </thead>
                    <tbody >
                        <tr >
                            <td>ID</td>
                            <td >{{ language.id }}</td> 
                        </tr>
                        <tr >
                            <td>Nom</td> 
                            <td >{{ language.name }}</td> 
                        </tr>
                        <tr >
                            <td>Code</td>  
                            <td >{{ language.code }}</td>
                        </tr>
                    </tbody>
                </table>
            <div class="flex justify-center items-center gap-5">
            
 
                <a href="{{ path('admin_language_edit', {'id': language.id}) }}" class="btn">
 
                        Modifier
                 </a>
             {{ include('admin/language/_delete_form.html.twig') }}
</div>
        </section>
 {% endblock %}
