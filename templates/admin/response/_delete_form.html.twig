<form method="post" action={{ path('admin_response_delete', {id: response.id} ) }} class="flex justify-center items-center" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ response.id) }}">
	<button type="submit" class="text-red-700 hover:underline">
		Supprimer
	</button>
</form>
