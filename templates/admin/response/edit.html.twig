{% extends 'admin/base_admin.html.twig' %}

{% block admin_title %}Modifier {{response.id}}{% endblock %}
 {% set return_path = 'admin_response_show' %} 
 {% set return_params = { id: response.id } %}


{% block admin_content %} 
{{ form_start(form) }}

    <table>
        <thead>
            <tr>
                <th>Catégorie</th>
                <th>Valeur</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Id</td>
                <td>{{response.id}}</td>
            </tr>
            <tr>
                <td>Status</td> 
                <td>{{ form_widget(form.status) }}</td>
            </tr>
             <tr>
                <td>Auteur</td>
                <td>{{ form_widget(form.author) }}</td>
            </tr>
            <tr>
                <td>Topic</td>
                <td>{{ form_widget(form.content) }}</td>
            </tr> 
        </tbody>
    </table>
    <div class="flex justify-center items-center gap-5">
        <button type="submit" class="btn">
        {{ button_label|default('Enregistrer') }}
    </button>
             {{ include('admin/response/_delete_form.html.twig') }}
       </div>
        {{ form_end(form) }}

 {% endblock %}
