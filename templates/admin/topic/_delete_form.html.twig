<form method="post" action={{ path('admin_topic_delete', {id: topic.id} ) }} class="flex justify-center items-center" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ topic.id) }}">
	<button type="submit" class="text-red-700 hover:underline">
		Supprimer
	</button>
</form>
