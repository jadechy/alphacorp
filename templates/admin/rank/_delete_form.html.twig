<form method="post" action="{{ path('admin_rank_delete', {'id': rank.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rank.id) }}">
    <button class="btn">Delete</button>
</form>
