<form method="post" action="{{ path('admin_scream_delete', {'id': alpha_scream.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mx-auto w-fit">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alpha_scream.id) }}">
	<button class="btn-delete">Supprimer</button>
</form>
