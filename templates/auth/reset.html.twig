{% extends "base.html.twig" %}
{% block title %}Mot de passe perdu{% endblock %}

{% block h1 %}Mot de passe perdu ?{% endblock %}

{% block body %}

 
        <section class="w-xl mx-auto"> 
 
                         <p>Choisissez votre nouveau mot de passe</p>

                        <p class="text-sm text-neutral-300 italic">
                            Votre mot de passe doit contenir au moins 8 caractères, une majuscule, une minuscule et un chiffre.
                        </p>

                        <form method="POST" action="{{ path('app_reset', {'token': token}) }}" class="mt-10 w-xl">
                            {# <div>
                                <input name="email" disabled id="username" type="text" value="{{ email }}" placeholder="Adresse Email"/>
                            </div> #}
                                 <input name="password" id="password" type="password" placeholder="Nouveau mot de passe" class="input" />
                              
                     
                                <input name="repeat_password" id="repeat-password" type="password" placeholder="Resaisissez votre nouveau mot de passe" class="inputmy-6" />
                    
                            <input type="hidden" name="token" value="{{ token }}">
                            <div class="flex justify-end">
                                <button type="submit" class="btn mb-6 "> 
                                    Changer mon mot de passe
                                </button>
                            </div>
                            <div class="w-full flex justify-center">
                                 <a href="{{ path('app_login')}}" class="text-center text-sm text-gray-400 w-full mt-3 hover:underline hover:decoration-solid">
                                    Vous vous souvenez de votre mot de passe ? Connectez-vous
                                </a>
                            </div>
                         </form>
           
        </section>
 {% endblock %}