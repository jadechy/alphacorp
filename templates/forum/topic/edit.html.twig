{% extends 'base.html.twig' %}

{% block title %}Modifier {{topic.title}}{% endblock %}

{% block body %}
         <a href="{{ path("app_topic", {id:topic.id}) }}" class="btn-secondary">
            Retour à la liste
        </a>
         <section class="mt-8">
            {{ include('forum/topic/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            {{ include('forum/topic/_delete_form.html.twig') }}
            </div>
        </section>
 {% endblock %}
