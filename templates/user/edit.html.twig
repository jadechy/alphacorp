{% extends 'base.html.twig' %}

{% block title %}Profile
{% endblock %}
{% block h1 %}Mon profile - Édition
{% endblock %}

{% block body %}
	<div>
		<!-- Section Profil -->
		<section class="flex gap-6 w-full">
			<img src={{ asset('img/uploads/' ~ user.image ) }} class="md:w-80 object-cover rounded-sm h-full w-full" alt="Profile Picture"/>

			{{ form_start(form, {"attr":{"class":"w-full"}}) }}
			{{ form_widget(form, {'attr': {'class': 'flex flex-col justify-between gap-3 w-full'}}) }}

			<button type="submit" class="btn mt-3">
				{{ button_label|default('Enregistrer') }}
			</button>
			{{ form_end(form) }}


		</section>


	</div>
{% endblock %}
