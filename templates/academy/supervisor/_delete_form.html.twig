<form method="post" action="{{ path('app_supervisor_academy_delete', {'id': academy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mx-auto w-fit">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ academy.id) }}">
	<button class="btn-delete">Supprimer</button>
</form>
