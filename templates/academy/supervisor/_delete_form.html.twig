<form method="post" action="{{ path('app_supervisor_academy_delete', {'id': academy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ academy.id) }}">
    <button class="btn">Delete</button>
</form>
