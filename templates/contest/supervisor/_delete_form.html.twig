<form method="post" action="{{ path('app_contest_supervisor_delete', {'id': contest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="w-fit mx-auto mt-4">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contest.id) }}">
	<button class="btn-delete ">Supprimer</button>
</form>
