<article class="shadow-xl shadow-neutral-900 rounded-xl p-10 mt-20">
	<div class="flex flex-col items-center gap-4">
		{{ ux_icon('emojione-monotone:lion-face', {class: 'w-12 h-12 text-fuchsia-500 '}) }}
		<h2 class="text-3xl">
			Cri de Guerre Alpha</h1>
	</h2>

	{# <div class="h-2 rounded-full w-60 bg-white"></div> #}
	<button id="startRecording">
		{{ ux_icon('tabler:player-play-filled', {class: 'w-7 h-7 hover:text-fuchsia-300'}) }}
	</button>
	<button id="stopRecording" class="hidden" disabled>
		{{ ux_icon('tabler:player-record-filled', {class: 'w-7 h-7 text-red-300 hover:text-fuchsia-300'}) }}
	</button>


	{# <audio id="audioPlayer" controls></audio> #}
	<p id="scoreResult" class="hidden"></p>
	<p id="levelResult" class="hidden"></p>

	<a href="{{ path('app_scream_leaderboard') }}" class="btn-secondary">Voir les meilleures résultats</a>
</div></article>
