<section class="flex gap-6 border rounded-lg w-full shadow-md shadow-gray-600 hover:border-neutral-300 border-2 hover:bg-neutral-700 transition">
	<img src="{{asset('img/1.png')}}" class="w-80   object-cover "/>
	<div class="p-6">
		<p class="text-2xl font-bold">{{title}}</p>
		<p class="max-h-40 truncate whitespace-pre-wrap">{{description}}</p>
	</div>
</section>
